[literal]
gem install foobar
gem --version
>>>
Document
  { docMeta =
      Meta
        { docTitle = []
        , docAuthors = []
        , docRevision = Nothing
        , docAttributes = fromList [ ( "sectids" , "" ) ]
        }
  , docBlocks =
      [ Block
          mempty Nothing (LiteralBlock "gem install foobar\ngem --version\n")
      ]
  }
